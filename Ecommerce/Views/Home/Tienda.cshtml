@model IEnumerable<Ecommerce.Models.Productos>

@{
    ViewBag.Title = "Tienda";
}

<div class="text-center">
    <h1>Catalogo</h1>
</div>
<ul class="nav justify-content-between px-5">
    @foreach (var catalog in ViewBag.catalogos)
    {
    <li class="nav-item">
        <a href="@Url.Action("Index", "Home",)" class="navbar-brand" title="List View">  
            <img alt="List View" src="@Url.Content("~/Content/img/logo.png")" class="img-logo">
        </a>
    </li>
    }


</ul>